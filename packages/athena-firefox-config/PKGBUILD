pkgname=athena-firefox-config
pkgver=1.0.0
pkgrel=3
pkgdesc="Mozilla Firefox configuration for Athena."
arch=('any')
license=("custom")
depends=('athena-nighttab' 'firefox-esr' 'profile-sync-daemon')
source=("$pkgname.tar.gz")

package() {
  install -dm 700 "${pkgdir}/etc/skel/.mozilla/firefox-esr/iso1fa16.default-release"
  install -dm 755 "${pkgdir}/etc/skel/.config/psd"

  cp -rf "${srcdir}/etc/skel/.mozilla" "${pkgdir}/etc/skel/"
  cp -rf "${srcdir}/etc/skel/.config/psd/psd.conf" "${pkgdir}/etc/skel/.config/psd/"
}
sha256sums=('cad60be0e5d335e8bd45ccbb0343601b059957b2b392e07eb37909f856ab0709')
